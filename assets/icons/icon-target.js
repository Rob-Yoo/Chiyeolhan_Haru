import { createIconSetFromFontello } from 'react-native-vector-icons';
import fontelloConfig from './icon-target.json';
const Icon = createIconSetFromFontello(fontelloConfig, 'icon-target');
export default Icon;
