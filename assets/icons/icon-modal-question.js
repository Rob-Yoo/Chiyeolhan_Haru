import { createIconSetFromFontello } from 'react-native-vector-icons';
import fontelloConfig from './icon-modal-question.json';
const Icon = createIconSetFromFontello(fontelloConfig, 'icon-modal-question');
export default Icon;
