import { createIconSetFromFontello } from 'react-native-vector-icons';
import fontelloConfig from './icon-schedule-pointer.json';
const Icon = createIconSetFromFontello(fontelloConfig, 'icon-schedule-pointer');
export default Icon;
