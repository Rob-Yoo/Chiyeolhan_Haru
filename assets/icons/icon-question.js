import { createIconSetFromFontello } from 'react-native-vector-icons';
import fontelloConfig from './icon-question.json';
const Icon = createIconSetFromFontello(fontelloConfig, 'icon-question');
export default Icon;
